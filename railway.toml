[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "y-sweet serve --host 0.0.0.0 --prod"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
